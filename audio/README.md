# audio

Archivos estáticos opcionales. Todo lo que pongas en `static/` se sirve desde la raíz
(por ejemplo `static/audio/archivo.png` → `audio/archivo.png`).
- Sonidos propios: ver comentario en `src/utils/sound.js`.
